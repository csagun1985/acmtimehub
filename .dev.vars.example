# Local Wrangler / OpenNext preview vars (optional)
# Copy to .dev.vars — never commit real secrets.

AUTH_SECRET=local-dev-secret-change-me
AUTH_URL=http://localhost:8787
AUTH_TRUST_HOST=true
# USE_D1=1
